import icetrollSprite from './ice_troll.png';
//spiders would damage you for 1 if touched by adjacent square, otherwise
// eslint-disable-next-line to the line before.
const iceTroll = {
  hp: 45,
  maxHp: 45,
  damage: 10,
  defence: 5,
  exp: 400,
  type: 'icetroll',
  sprite: icetrollSprite
};

export default iceTroll;
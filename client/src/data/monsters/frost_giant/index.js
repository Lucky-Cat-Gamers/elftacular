import frostgiantSprite from './ice_goblin1.png';
//spiders would damage you for 1 if touched by adjacent square, otherwise
// eslint-disable-next-line to the line before.
const frostGiant = {
  hp: 60,
  maxHp: 60,
  damage: 12,
  defence: 7,
  exp: 500,
  type: 'frostgiant',
  sprite: frostgiantSprite
};

export default frostGiant;